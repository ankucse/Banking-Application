server:
  port: 8082
spring:
  application:
    name: onboarding-service
  data:
    mongodb:
      uri: mongodb://mongoadmin:mongopassword@localhost:27017/onboarding_db?authSource=admin
  kafka:
    bootstrap-servers: localhost:9092
